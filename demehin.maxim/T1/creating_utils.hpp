#ifndef CREATING_UTILS_HPP
#define CREATING_UTILS_HPP
#include <cstddef>
#include "base-types.hpp"

namespace demehin::details
{
  point_t* copyCords(const point_t* vertex, size_t vrt_cnt);
}

#endif
