#ifndef RECURSION_HPP
#define RECURSION_HPP
namespace evstyunichev
{
  const char * IsFactorCorrect(const char *, int prev = 3);
  const char * IsTermCorrect(const char *, int prev = 0);
  bool IsDigit(char);
  bool IsLetter(char);
  bool IsBasicSymbol(char);
}
#endif