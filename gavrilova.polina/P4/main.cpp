#include <iostream>
#include <cctype>

char * rm_digits (const char * original, char * new_str)
{
  size_t j = 0;
  for (size_t i = 0; i != "\0"; ++i) {
    if (!isdigit) {
      new_str[j] = original[i];
    }
    ++j;
  }
}

int main()
{
}
