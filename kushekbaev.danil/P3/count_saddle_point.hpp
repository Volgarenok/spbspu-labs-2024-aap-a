#ifndef COUNT_SADDLE_POINT_HPP
#define COUNT_SADDLE_POINT_HPP
#include <cstddef>

namespace kushekbaev
{
  int countSaddlePoints(
    const int* matrix,
    size_t rows,
    size_t columns
  );
}

#endif
