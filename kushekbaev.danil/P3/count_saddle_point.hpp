#ifndef COUNT_SADDLE_POINT_HPP
#define COUNT_SADDLE_POINT_HPP
#include <cstddef>

namespace kushekbaev
{
  int countSaddlePoints(const int* matrix, const size_t rows, const size_t columns);
}

#endif
