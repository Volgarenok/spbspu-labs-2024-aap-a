#ifndef HAS_DUPLICATES_IN_COLUMN_HPP
#define HAS_DUPLICATES_IN_COLUMN_HPP

#include <cstddef>

namespace maslovskiy
{
  bool hasDuplicatesInColumn(const int *matrix, size_t rows, size_t col, size_t cols);
}

#endif
